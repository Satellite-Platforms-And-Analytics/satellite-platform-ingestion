# Satellite Platform - Ingestion Pipeline: core runtime
# Python 3.11+
#
#   pip install -r requirements.txt
#
# This file is what every scheduled workflow installs. Keep it to what the
# pipeline entry points under src/ actually import, and nothing else - the
# whole point of the split is that this list installs fast and cannot fail
# on a native dependency.
#
# Optional groups, installed only where they are needed:
#
#   requirements-imagery.txt    src/imagery/  (GDAL-backed, slow, no
#                               scheduled job runs it yet)
#   requirements-tracking.txt   src/tracking/ (the interactive Satellite
#                               Visibility Tool's reporting stack)
#   requirements-dev.txt        tests and linting
#
# Audited against actual imports on 2026-09-10. The previous version of
# this file listed aiohttp, geopandas, shapely, pyproj, fiona, dask,
# xarray, alembic, apscheduler and pytest-asyncio - ten packages that no
# module in this repository imports. Four of them were cited by name in
# five workflow headers as the reason those workflows could not use this
# file. They were removed rather than pinned. tests/test_requirements.py
# now fails if this drifts again in either direction.

# HTTP
requests>=2.31.0

# Orbit propagation and geometry
sgp4>=2.22
skyfield>=1.45
numpy>=1.24

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Environment
python-dotenv>=1.0.0
